{
  "devDependencies": {
    "sitemap": "^8.0.0",
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
	"push": "git add . && git commit -m \"📝 Auto-deploy: $(date +'%Y-%m-%d %H:%M:%S')\" && git push"
  }
}